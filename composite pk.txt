when a 2 columns comes in a multiple duplicates we can combain the columns and give the pk for that particular column its called composite pk or composite key

ex
d-in        e_id        name        sal
10          1           abc         10000
10          2           def         8000
10          3           hvn         13000
20          1           uiu         20000
20          2           uiu         20000
30          1           uiu         20000
30          2           uiu         20000


create table index_name on emp (d_id,e_id);

alter table table_name addd constaraint com_pk primary key on (d_id,e_id);

create table table_name(
    col datatype,
    col datatype
    col datatype
    col datatype
    col datatype

    constaraint com_pk primary key on (d_id,e_id)
);

